## Summary

<!-- What does this PR do, and why? -->

## Component

- [ ] Rust TUI (`src/tui`)
- [ ] Python package (`src/ros2_fastfetch`)
- [ ] Docs / plugin API

## Checklist

- [ ] `cargo build` / `cargo test` / `cargo clippy --all-targets -- -D warnings` pass (Rust)
- [ ] `pytest` / `flake8` pass (Python)
- [ ] If adding a plugin: implemented `Plugin`, added a unit test, registered in `register_builtins` (`docs/plugins.md`)
- [ ] Updated `features.md` / `README.md` for user-facing changes

## Related issue

<!-- e.g. Closes #123 -->
